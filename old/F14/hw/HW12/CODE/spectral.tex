% solve u_t = u_xx + f
% with boundary conditions u(0,t) = u(1,t) = 0 
% and initial conditions u(x,0) = exp(x-t) x (1-x)
% using finite elements with phi_k(x) = sqrt(2) sin(k pi x)


 n = 16;
 M = eye(n);
 K = diag([1:n].^2*3*(pi^2)/2;
 for j=1:n, for k=1:n
    if j~=k, K(j,k) = (-1+(-1)^(j+k))*j*k*(j^2+k^2)/((j^2-k^2)^2);
 end, end

  
